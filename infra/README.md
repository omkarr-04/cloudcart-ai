# Infra

This folder will contain containerization and orchestration artifacts for CloudCart AI.

- `docker/` — Dockerfiles and development compose files
- `kubernetes/` — manifests and deployment configs
- `nginx/` — reverse proxy and ingress configuration
